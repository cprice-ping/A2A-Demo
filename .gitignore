# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/

# Env / secrets
.env
.env.local
*.json.key
adc.json

# OS / editor
.DS_Store
.idea/
.vscode/
*.tsbuildinfo
*.swp

# TokenExchange-AS real credentials (k8s manifests live here; secrets never)
token-exchange/secret.yaml

# Impeccable working artifacts + product/design notes (session tooling)
.impeccable/
DESIGN.md
PRODUCT.md

# Stray npm artifact at repo root (the UI has its own package.json/lock)
/package-lock.json

# TokenExchange-AS content lives in its own repo; this dir only mirrors
# deployment manifests + holds a real secret that must never be committed
token-exchange/
