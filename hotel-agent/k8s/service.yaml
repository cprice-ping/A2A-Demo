apiVersion: v1
kind: Service
metadata:
  name: hotel-agent
  namespace: a2a-demo
  labels:
    app: hotel-agent
spec:
  type: ClusterIP
  selector:
    app: hotel-agent
  ports:
    - port: 80
      targetPort: 8080
